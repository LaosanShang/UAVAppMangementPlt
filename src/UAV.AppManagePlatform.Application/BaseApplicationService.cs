using Ls.Application;
using System;
using System.Collections.Generic;

namespace UAV.AppManagePlatform.Application
{
    public class BaseApplicationService : ApplicationServiceBase
    {
        public BaseApplicationService()
        {
        }

        #region Combo
        //protected List<ComboboxItem> GetEnumValues<TEnum>()
        //{
        //    List<ComboboxItem> results = new List<ComboboxItem>();
        //    foreach (TEnum value in Enum.GetValues(typeof(TEnum)))
        //    {
        //        results.Add(new ComboboxItem { Id = value.GetHashCode().ToString(), Text = value.ToString() });
        //    }
        //    return results;
        //}
        #endregion
    }
}
